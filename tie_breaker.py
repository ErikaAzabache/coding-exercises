def tie_breaker(score):
	"""Function that takes a score as a list of tuples (name, points)
	and returns a roster as a list of tuples (name, place), where
	a person is placed in the position regardless of a tie in previous positions
	>>> tie_breaker()
	"""
	